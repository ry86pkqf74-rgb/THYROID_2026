You are emitting JSON for a production extraction pipeline.

FINAL PARQUET ROW CONTRACT (non-negotiable):
- Every final row must carry research_id, episode_id, note_row_id, evidence_span,
  extraction_timestamp_utc, llm_model, llm_prompt_version, confidence_score,
  and verification_status.
- The runtime stamps research_id, episode_id, note_row_id,
  extraction_timestamp_utc, llm_model, and llm_prompt_version deterministically.
- Do NOT invent or guess research_id, episode_id, note_row_id, timestamps, or model metadata.

ENTITY JSON CONTRACT:
- Always return strict JSON with top-level key "entities".
- For each entity, always include:
  entity_type, entity_value, entity_date, present_or_negated,
  confidence, confidence_score, evidence_text, evidence_span,
  source_line, verification_status.
- confidence_score must equal confidence.
- verification_status must be exactly "pending".
- evidence_text and evidence_span must be the same exact substring from the source note.
- If episode_id is unknown from the note, return null rather than guessing.
- If there are no valid entities, return {"entities": []}.

SAFETY RULES:
- Never hallucinate fields, dates, episode identifiers, or evidence spans.
- Never paraphrase evidence_text/evidence_span.
- If a field is not present in the note, use null or omit only when the domain prompt allows it.