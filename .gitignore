*.duckdb
.DS_Store
__pycache__/
*.pyc
.streamlit/secrets.toml
.streamlit/_token_import.oneoff
# Cursor IDE — ignore local IDE state; track `.cursor/skills/**` only (no secrets in skills)
.cursor/*
!.cursor/skills/
!.cursor/skills/**
.venv/
.vscode/

# ── Secrets — NEVER commit ────────────────────────────────────────────────
.env
*.env
!.env.example
.env.motherduck

# MotherDuck tokens (TOML — same keys as .streamlit/secrets.toml; see motherduck.local.toml.example)
# Canonical local store for PAT / SA token (reference for scripts --md); never commit.
motherduck.local.toml
motherduck.local.toml.backup
motherduck.local.secrets.toml

# Raw source data — PHI-sensitive, must not be committed
raw/

# Processed Parquet files (generated by pipeline, DVC-tracked)
processed/*.parquet

# Large generated CSV exports (not committed)
processed/clinical_notes_long.csv
processed/clinical_notes_long_qa.csv

# ── Exports (all generated/reproducible) ──────────────────────
# Timestamped export bundles — reproducible from scripts
exports/FINAL_PUBLICATION_BUNDLE_*/
exports/MANUSCRIPT_FREEZE_BUNDLE_*/
exports/manuscript_cohort_*/
exports/manuscript_cohort_freeze/
exports/manuscript_analysis/
exports/manuscript_reconciliation_*/
exports/hardening_audit_*/
exports/operative_notes_full_history_*/
exports/thyroseq_integration_*/
exports/afirma_integration_*/
exports/dicom_header_ingest_*/
exports/ata_primary_source_scan_*/
exports/publication_figures_300dpi/
exports/latex_tables/
exports/dataset_maturation_*/

# Zenodo archive snapshots — published at DOI 10.5281/zenodo.18945510
exports/zenodo_archive_*/
exports/zenodo_archive_*.zip

# Temporary export artifacts
exports/*_temp/
exports/*_draft/
exports/validation_run_*/
exports/motherduck_gold_daily_*/
exports/motherduck_provenance_*/
exports/final_master_release_*/
exports/parquet_release_*/
exports/mm_contract_summary_*.json
exports/fhir_specimen_*/

# ── Root-level manuscript/freeze ZIPs (reproducible) ──────────
THYROID_2026_MANUSCRIPT_PACKAGE_*.zip
THYROID_2026_MANUSCRIPT_FREEZE_*.zip
exports/THYROID_2026_MANUSCRIPT_FREEZE_*.zip

# ── Logs (ephemeral) ─────────────────────────────────────────
logs/

# ── LLM extraction intermediate artifacts ───────────────────
llm_extraction/audit_*.parquet
llm_extraction/rln_*.parquet
llm_extraction/missed_data_sweep_*.parquet
llm_extraction/phase*_results_*.json
llm_extraction/phase*_preop_molecular_refinement_*.md

# ── Study outputs with timestamped duplicates ─────────────────
studies/manuscript_package_*/
# Append-only MotherDuck pipeline audit (generated locally; no secrets)
studies/molecular_pipeline_audit_log.md

# ── LLM extraction validation run artifacts (parquets too large) ──
studies/llm_extraction_validation/runs/*/
studies/v2_domain_promotion_gate_*/llm_side_by_side.parquet
studies/md_gate_dryrun_*/llm_side_by_side.parquet
studies/md_gate_dryrun_*/llm_side_by_side.csv
studies/md_stage_refresh_*/gate/llm_side_by_side.parquet
studies/md_stage_refresh_*/gate/llm_side_by_side.csv

# MotherDuck live release audit — large gate exports (reproducible via 112/124)
studies/*_motherduck_live_release_audit/promotion_gate/llm_side_by_side.parquet
studies/*_motherduck_live_release_audit/promotion_gate/llm_side_by_side.csv
studies/*_motherduck_live_release_audit/promotion_gate/manual_review_queue.csv
studies/*_motherduck_live_release_audit/manual_review_queue.csv

# ── Fact lineage QC reports (ephemeral, timestamped) ──────────
exports/fact_lineage_qc/
exports/review_queue_triage_*/
exports/fna_bethesda_residual_worklist_*/

# ── Extraction pipeline: PHI-sensitive data (never commit) ────
processed/remaining/*.parquet
processed/remaining/*.csv
processed/output/*.parquet

# ── Extraction pipeline: checkpoint & output data (OK to commit) ──
# processed/output/*.ckpt.jsonl — may be committed
# processed/output/server_checkpoints/ — committed
# processed/output/v2_backup/ — committed
output/parquet_backup/
