# Copy this file to secrets.toml and replace the placeholder:
#   cp secrets.toml.example secrets.toml
#
# secrets.toml is gitignored — never commit real tokens.
# For Streamlit Community Cloud, paste this value into
# App Settings → Secrets in the web UI instead.

MOTHERDUCK_TOKEN = "your_motherduck_token_here"
