<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `canonical_operative_patient_rollup_v1`

**Grain:** One row per patient (rollup summaries)

**Total rows:** `10,871`

**Distinct patients:** `10,871`

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/95b_rollup_derivation_signoff.sql`

## Purpose

Mass-equivalence re-derivation against verified upstream events table; >99% match on simple aggregations, complex derived cols deterministic from upstream per build script.

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/95b_rollup_derivation_signoff.sql`; Tier `tier2_rollups`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `n_surgeries` — verification category `derived` registry seed
- Column `n_total_thyroidectomies` — verification category `derived` registry seed
- Column `n_hemithyroidectomies` — verification category `derived` registry seed
- Column `n_completion_thyroidectomies` — verification category `derived` registry seed
- Column `n_central_neck_dissections` — verification category `derived` registry seed
- Column `n_lateral_neck_dissections` — verification category `derived` registry seed
- Column `any_reoperative_field` — verification category `derived` registry seed
- Column `any_parathyroid_autograft` — verification category `derived` registry seed
- Column `total_parathyroid_autograft_count` — verification category `derived` registry seed
- Column `total_parathyroid_identified_count` — verification category `derived` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_identifier_skip`
- `auto_provenance_skip`
- `mechanical_derivation_compare against verified events table`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
