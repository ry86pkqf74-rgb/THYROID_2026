<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `molecular_variants_unnested_VIEW_v2`

**Grain:** See table keys in data dictionary

**Total rows:** unavailable — MotherDuck could not SCAN this object (recover view DDL / re-deploy).

*Binder detail (trimmed):* `Binder Error: Contents of view were altered: types don't match! Expected [VARCHAR, BIGINT, TIMESTAMP, VARCHAR, VARCHAR, VARCHAR, VARCHAR, INTEGER, VARCHAR, VARCHAR, BOOLEAN, DOUBLE, VARCHAR], but found [VARCHAR, BIGINT, `

**Distinct patients:** unavailable (same COUNT error as row total)

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/164b_view_layer_molecular_unnest_register_20260429.sql`

## Purpose

| mig_164b: seeded table row for molecular UNNEST VIEW layer (mig_164 gap closure). | mig_164b: molecular UNNEST VIEW signoff (mig_164 gap closure).

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/164b_view_layer_molecular_unnest_register_20260429.sql`; Tier `tier2_canonical_view`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `research_id` — verification category `na_provenance` registry seed
- Column `molecular_episode_id` — verification category `derived` registry seed
- Column `test_date_native` — verification category `derived` registry seed
- Column `platform` — verification category `derived` registry seed
- Column `gene` — verification category `derived` registry seed
- Column `protein` — verification category `derived` registry seed
- Column `cdna` — verification category `derived` registry seed
- Column `af_pct` — verification category `derived` registry seed
- Column `source_call` — verification category `derived` registry seed
- Column `mutations_status_for_test` — verification category `derived` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_view_unnest_canonical_molecular_genetics_v2_gene_mutations_variants`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
