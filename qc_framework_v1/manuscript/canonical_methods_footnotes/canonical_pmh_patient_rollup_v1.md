<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `canonical_pmh_patient_rollup_v1`

**Grain:** One row per patient (rollup summaries)

**Total rows:** `10,871`

**Distinct patients:** `10,871`

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/114_pmh_patient_rollup_signoff.sql`

## Purpose

Rebuilt stale PMH patient rollup with Script 365 PMH logic after mig_107 verified canonical_pmh_events_v1. Pre-rebuild rollup build_ts was 2026-04-22; events now 12,696 rows / 4,158 patients after +252 synthetic rows. Post-rebuild validation: 10,871 rows matching CPM and 0 drift across 77 derivable columns vs fresh re-derivation. PMH family closed: events (mig_107) + rollup (mig_114).

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/114_pmh_patient_rollup_signoff.sql`; Tier `tier2_rollups`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `anchor_source` — verification category `adjudicated` registry seed
- Column `n_findings_any` — verification category `derived` registry seed
- Column `n_findings_present` — verification category `derived` registry seed
- Column `n_findings_definitive` — verification category `derived` registry seed
- Column `n_findings_probable_or_better` — verification category `derived` registry seed
- Column `first_finding_date` — verification category `adjudicated` registry seed
- Column `last_finding_date` — verification category `adjudicated` registry seed
- Column `n_distinct_findings_norm` — verification category `derived` registry seed
- Column `pmh_diabetes_definitive` — verification category `adjudicated` registry seed
- Column `pmh_diabetes_probable_or_better` — verification category `adjudicated` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_identifier_skip`
- `auto_provenance_skip`
- `derivation_re_derivation_post_events_repair`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
