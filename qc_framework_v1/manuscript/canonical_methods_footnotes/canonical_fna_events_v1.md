<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `canonical_fna_events_v1`

**Grain:** Event grain (multiple rows per patient possible)

**Total rows:** `8,050`

**Distinct patients:** `5,229`

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/96_fna_days_to_surgery_recompute.sql`

## Purpose

Verified canonical `canonical_fna_events_v1` in publication DB (`thyroid_canonical_publication_v1_0`). Operational meaning is anchored in Logan verification batches referenced on column rows.

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/96_fna_days_to_surgery_recompute.sql`; Tier `pilot`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `fna_event_id` — verification category `na_provenance` registry seed
- Column `research_id` — verification category `na_provenance` registry seed
- Column `fna_index` — verification category `derived` registry seed
- Column `fna_seq_n` — verification category `derived` registry seed
- Column `fna_total_n_for_patient` — verification category `derived` registry seed
- Column `is_first_fna` — verification category `derived` registry seed
- Column `is_last_fna` — verification category `derived` registry seed
- Column `fna_date_raw` — verification category `source` registry seed
- Column `fna_date_resolved` — verification category `derived` registry seed
- Column `fna_date_status` — verification category `na_provenance` registry seed

## Known limitations

- **`CF-FNA-D2S`:** carry-forward / limitation referenced in registry notes.
- **Representative excerpts (verbatim trims):**
  - pure provenance: content-derived hash; no source counterpart; auto-verify at Step D table sign-off | mig_78: auto-verified at FNA pilot table sign-off (Step D); pure provenance/pipeline-trace, no source counterpart.
  - pure provenance: patient identifier; no source counterpart; auto-verify at Step D table sign-off | mig_78: auto-verified at FNA pilot table sign-off (Step D); pure provenance/pipeline-trace, no source counterpart.
  - mechanical_derivation_compare; rule: DENSE_RANK() OVER (PARTITION BY research_id ORDER BY fna_date_resolved) | mig_78: source-workbook column position 1..12 (NOT chronological); chronological order is in fna_seq_n. Stab…
  - mechanical_derivation_compare; rule: ROW_NUMBER() OVER (PARTITION BY research_id ORDER BY fna_date_resolved) | recomputed in mig_70 from cleaned fna_date_raw (MM/DD/YYYY); rule: ROW_NUMBER chronological with fna_event_i…
  - mechanical_derivation_compare; rule: COUNT(*) OVER (PARTITION BY research_id) | recomputed in mig_70 from cleaned fna_date_raw (MM/DD/YYYY); rule: ROW_NUMBER chronological with fna_event_id tiebreaker for same-date ties.
  - mechanical_derivation_compare; rule: fna_seq_n = 1 | recomputed in mig_70 from cleaned fna_date_raw (MM/DD/YYYY); rule: ROW_NUMBER chronological with fna_event_id tiebreaker for same-date ties.
  - mechanical_derivation_compare; rule: fna_seq_n = fna_total_n_for_patient | recomputed in mig_70 from cleaned fna_date_raw (MM/DD/YYYY); rule: ROW_NUMBER chronological with fna_event_id tiebreaker for same-date ties.
  - mechanical_source_compare → FNAs 12_5_2025.xlsx > FNA Bethesda > Date cell (per FNA index 1..12) | verified by mechanical_source_compare against FNAs 12_5_2025.xlsx > FNA Bethesda; 8042 MATCH, 72 AMBIGUOUS (raw-cell agr…


## Verification methods used

- `auto_no_source_counterpart`
- `manual_source_review`
- `mechanical_derivation_compare`
- `mechanical_source_compare`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
