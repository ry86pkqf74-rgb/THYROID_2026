<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `canonical_labs_vitamin_d_v1`

**Grain:** See table keys in data dictionary

**Total rows:** `86`

**Distinct patients:** `82`

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/115_labs_family_signoff.sql`

## Purpose

Labs family closed under Protocol v2 via Script 347 structured-source/normalizer replay. 45/45 normalizer tests passed; per-row replay found 0 value_numeric, is_censored, or value_correction_note drift across all five tables. Unit vocab and source enum checks clean. lab_datetime remains TIMESTAMP (all current rows midnight-valued from available source timestamps, but labs retain timestamp type for future draw-time fidelity). Conservative range exceptions documented as normalizer-plausible values.

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/115_labs_family_signoff.sql`; Tier `tier2_canonical`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `lab_datetime` — verification category `adjudicated` registry seed
- Column `value_raw` — verification category `source` registry seed
- Column `value_numeric` — verification category `adjudicated` registry seed
- Column `is_censored` — verification category `derived` registry seed
- Column `value_correction_note` — verification category `adjudicated` registry seed
- Column `unit_standardized` — verification category `adjudicated` registry seed
- Column `source` — verification category `adjudicated` registry seed
- Column `is_in_canonical_cancer_cohort` — verification category `derived` registry seed
- Column `ingestion_date` — verification category `adjudicated` registry seed
- Column `research_id` — verification category `na_provenance` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_identifier_skip`
- `structured_source_compare_with_normalizer`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
