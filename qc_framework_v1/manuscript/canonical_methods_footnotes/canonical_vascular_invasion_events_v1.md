<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `canonical_vascular_invasion_events_v1`

**Grain:** Event grain (multiple rows per patient possible)

**Total rows:** `3,861`

**Distinct patients:** `3,745`

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/94_vascular_invasion_table_signoff.sql`

## Purpose

Verified canonical `canonical_vascular_invasion_events_v1` in publication DB (`thyroid_canonical_publication_v1_0`). Operational meaning is anchored in Logan verification batches referenced on column rows.

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/94_vascular_invasion_table_signoff.sql`; Tier `tier1_events`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `vi_event_id` — verification category `na_provenance` registry seed
- Column `research_id` — verification category `na_provenance` registry seed
- Column `note_type` — verification category `na_provenance` registry seed
- Column `note_index` — verification category `na_provenance` registry seed
- Column `source_workbook` — verification category `na_provenance` registry seed
- Column `source_sheet` — verification category `na_provenance` registry seed
- Column `source_column` — verification category `na_provenance` registry seed
- Column `vascular_invasion` — verification category `adjudicated` registry seed
- Column `vascular_invasion_extent` — verification category `adjudicated` registry seed
- Column `vessel_count` — verification category `derived` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_no_source_counterpart`
- `manual_source_review`
- `mechanical_derivation_compare`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
