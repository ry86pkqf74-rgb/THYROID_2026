<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `canonical_t4b_invasion_patient_rollup_v1`

**Grain:** One row per patient (rollup summaries)

**Total rows:** `434`

**Distinct patients:** `434`

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/95_ete_taxonomy_and_invasion_rollups.sql`

## Purpose

| mig_95: invasion-family rollup verified after ETE taxonomy correction.

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/95_ete_taxonomy_and_invasion_rollups.sql`; Tier `tier2_rollups`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `n_events` — verification category `derived` registry seed
- Column `any_prevertebral_fascia` — verification category `derived` registry seed
- Column `any_carotid_encasement` — verification category `derived` registry seed
- Column `any_mediastinal_vessel` — verification category `derived` registry seed
- Column `any_pT4b_direct` — verification category `derived` registry seed
- Column `any_pT4b_final` — verification category `derived` registry seed
- Column `n_pT4b_events` — verification category `derived` registry seed
- Column `n_prevertebral_events` — verification category `derived` registry seed
- Column `n_carotid_events` — verification category `derived` registry seed
- Column `n_mediastinal_events` — verification category `derived` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_identifier_skip`
- `auto_provenance_skip`
- `mechanical_derivation_compare`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
