<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `canonical_path_gland_patient_rollup_v1`

**Grain:** One row per patient (rollup summaries)

**Total rows:** `10,731`

**Distinct patients:** `10,731`

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/101_path_gland_patient_rollup_signoff.sql`

## Purpose

Rebuilt via Script 361 derivation logic post-Cursor events repair (06f88ae). All 10 derived cols match fresh re-derivation. Pre-rebuild diff isolated to n_parathyroid_glands_documented (parathyroid taxonomy renormalization side-effect). build_ts retyped from TIMESTAMP WITH TIME ZONE to TIMESTAMP.

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/101_path_gland_patient_rollup_signoff.sql`; Tier `tier2_rollups`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `any_thyroid_lobe_measured` — verification category `derived` registry seed
- Column `total_thyroid_weight_g` — verification category `adjudicated` registry seed
- Column `left_lobe_max_dim_cm` — verification category `adjudicated` registry seed
- Column `right_lobe_max_dim_cm` — verification category `adjudicated` registry seed
- Column `any_isthmus_documented` — verification category `derived` registry seed
- Column `any_parathyroid_documented` — verification category `derived` registry seed
- Column `n_parathyroid_glands_documented` — verification category `derived` registry seed
- Column `n_parathyroid_glands_abnormal` — verification category `derived` registry seed
- Column `any_parathyroid_hyperplasia` — verification category `derived` registry seed
- Column `any_parathyroid_adenoma` — verification category `derived` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_identifier_skip`
- `auto_provenance_skip`
- `derivation_re_derivation_post_events_repair`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
