<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `canonical_cervical_ln_clinical_patient_rollup_v1`

**Grain:** One row per patient (rollup summaries)

**Total rows:** `1,643`

**Distinct patients:** `1,643`

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/113_cervical_ln_clinical_patient_rollup_signoff.sql`

## Purpose

Derivation re-derivation against verified canonical_cervical_ln_clinical_events_v1 (mig_111). 7/7 cols 0 drift on 1,643 patients. Events-scope rollup. cervical_ln_clinical family complete (events mig_111 + rollup mig_113).

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/113_cervical_ln_clinical_patient_rollup_signoff.sql`; Tier `tier2_rollups`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `n_entities` — verification category `derived` registry seed
- Column `n_distinct_entity_types` — verification category `derived` registry seed
- Column `n_notes_with_entities` — verification category `derived` registry seed
- Column `n_positive_ln_level_mentions` — verification category `derived` registry seed
- Column `n_ln_size_mentions` — verification category `derived` registry seed
- Column `n_fna_of_ln_mentions` — verification category `derived` registry seed
- Column `has_positive_ln_level` — verification category `derived` registry seed
- Column `research_id` — verification category `na_provenance` registry seed
- Column `build_ts` — verification category `na_provenance` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_identifier_skip`
- `auto_provenance_skip`
- `derivation_re_derivation_against_verified_events`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
