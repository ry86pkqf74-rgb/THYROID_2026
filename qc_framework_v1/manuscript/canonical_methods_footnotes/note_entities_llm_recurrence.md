<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `note_entities_llm_recurrence`

**Grain:** See table keys in data dictionary

**Total rows:** `11,037`

**Distinct patients:** `5,641`

**Verification status:** verified

**Signoff migration:** `registry_seed_raw_llm_mirror_exempt`

## Purpose

Raw LLM-output mirror table; exempt from per-col verification (no canonical adjudication required at this layer). Content is already QC-checked at LLM extraction time via the extractor's error column and downstream canonicalization into canonical_<domain>_events_v1 tables. Audited 2026-04-29 in cleanliness pass.

## Build pipeline

Rolling signoff `registry_seed_raw_llm_mirror_exempt`; Tier `tier3_extraction`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `note_row_id` — verification category `na_provenance` registry seed
- Column `domain` — verification category `na_provenance` registry seed
- Column `llm_model` — verification category `na_provenance` registry seed
- Column `llm_base_url` — verification category `na_provenance` registry seed
- Column `extracted_at` — verification category `na_provenance` registry seed
- Column `result_json` — verification category `na_provenance` registry seed
- Column `research_id` — verification category `na_provenance` registry seed
- Column `note_type` — verification category `na_provenance` registry seed
- Column `note_date` — verification category `na_provenance` registry seed
- Column `linkage_date` — verification category `na_provenance` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_identifier_skip`
- `auto_provenance_skip`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
