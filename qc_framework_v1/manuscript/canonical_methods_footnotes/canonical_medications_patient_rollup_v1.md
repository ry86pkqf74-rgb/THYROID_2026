<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `canonical_medications_patient_rollup_v1`

**Grain:** One row per patient (rollup summaries)

**Total rows:** `10,871`

**Distinct patients:** `10,871`

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/105_medications_patient_rollup_signoff.sql`

## Purpose

Derivation re-derivation against verified canonical_medications_events_v1 (mig_103 commit 3612128). Rollup was rebuilt by mig_103 apply script (Script 365 step 2). 1,820 with-meds pts: 0 drift across 15 derived metrics. 9,051 no-meds pts: 0 contamination. Cohort parity 10,871 = CPM. medications family closed: events (mig_103) + rollup (mig_104). 21st canonical under Protocol v2.

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/105_medications_patient_rollup_signoff.sql`; Tier `tier2_rollups`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `anchor_source` — verification category `adjudicated` registry seed
- Column `n_findings_any` — verification category `derived` registry seed
- Column `n_findings_present` — verification category `derived` registry seed
- Column `n_findings_definitive` — verification category `derived` registry seed
- Column `n_findings_probable_or_better` — verification category `derived` registry seed
- Column `first_finding_date` — verification category `adjudicated` registry seed
- Column `last_finding_date` — verification category `adjudicated` registry seed
- Column `n_distinct_findings_norm` — verification category `derived` registry seed
- Column `meds_levothyroxine_definitive` — verification category `adjudicated` registry seed
- Column `meds_levothyroxine_probable_or_better` — verification category `adjudicated` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_identifier_skip`
- `auto_provenance_skip`
- `derivation_re_derivation_against_verified_events`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
