<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `canonical_esophageal_invasion_patient_rollup_v1`

**Grain:** One row per patient (rollup summaries)

**Total rows:** `60`

**Distinct patients:** `60`

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/95_ete_taxonomy_and_invasion_rollups.sql`

## Purpose

| mig_95: invasion-family rollup verified after ETE taxonomy correction.

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/95_ete_taxonomy_and_invasion_rollups.sql`; Tier `tier2_rollups`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `n_entities` — verification category `derived` registry seed
- Column `n_distinct_entity_types` — verification category `derived` registry seed
- Column `n_notes_with_entities` — verification category `derived` registry seed
- Column `has_esophageal_invasion_present_true` — verification category `derived` registry seed
- Column `has_esophageal_repair` — verification category `derived` registry seed
- Column `has_muscularis_invasion` — verification category `derived` registry seed
- Column `has_mucosal_invasion` — verification category `derived` registry seed
- Column `max_invasion_extent` — verification category `adjudicated` registry seed
- Column `max_invasion_length_cm` — verification category `adjudicated` registry seed
- Column `research_id` — verification category `na_provenance` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_identifier_skip`
- `auto_provenance_skip`
- `mechanical_derivation_compare`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
