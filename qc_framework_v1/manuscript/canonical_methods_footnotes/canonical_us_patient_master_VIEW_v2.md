<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `canonical_us_patient_master_VIEW_v2`

**Grain:** See table keys in data dictionary

**Total rows:** unavailable — MotherDuck could not SCAN this object (recover view DDL / re-deploy).

*Binder detail (trimmed):* `Binder Error: Contents of view were altered: types don't match! Expected [INTEGER, BOOLEAN, BIGINT, DATE, DATE, BOOLEAN, VARCHAR, DOUBLE, VARCHAR, VARCHAR, HUGEINT, BOOLEAN, BOOLEAN, DATE, BOOLEAN, BOOLEAN, DATE, BOOLEAN`

**Distinct patients:** unavailable (same COUNT error as row total)

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/164_view_layer_registration_signoff_20260429.sql`

## Purpose

| mig_164: seeded table row for VIEW layer (Cowork VERIFY count vs information_schema.columns). | mig_164 VIEW layer CLOSE (canonical_us_* + molecular UNNEST VIEWs -- Protocol v2 sign-off rollup). Post-§11 Cowork audit gate uplift.

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/164_view_layer_registration_signoff_20260429.sql`; Tier `tier2_canonical_view`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `research_id` — verification category `na_provenance` registry seed
- Column `has_any_us` — verification category `derived` registry seed
- Column `n_us_exams` — verification category `derived` registry seed
- Column `first_us_date` — verification category `derived` registry seed
- Column `last_us_date` — verification category `derived` registry seed
- Column `preop_us_available_flag` — verification category `derived` registry seed
- Column `max_tirads_category_ever` — verification category `derived` registry seed
- Column `max_tirads_points_ever` — verification category `derived` registry seed
- Column `tirads_category_at_first_exam` — verification category `derived` registry seed
- Column `tirads_category_at_last_preop_exam` — verification category `derived` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_view_derived_us_patient_rollup_vs_exam_aggregate_VIEW_pipeline`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
