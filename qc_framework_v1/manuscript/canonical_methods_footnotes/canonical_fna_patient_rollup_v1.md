<!-- READY FOR LOGAN MANUSCRIPT REFINEMENT -->

# `canonical_fna_patient_rollup_v1`

**Grain:** One row per patient (rollup summaries)

**Total rows:** `5,266`

**Distinct patients:** `5,266`

**Verification status:** verified

**Signoff migration:** `qc_framework_v1/migrations/95b_rollup_derivation_signoff.sql`

## Purpose

Mass-equivalence re-derivation against verified upstream events table; >99% match on simple aggregations, complex derived cols deterministic from upstream per build script.

## Build pipeline

Rolling signoff `qc_framework_v1/migrations/95b_rollup_derivation_signoff.sql`; Tier `tier2_rollups`. Rebuild per latest Path-C batch; derivation scripts referenced alongside column-level verification_method entries in canonical_column_verification_registry_v1.

## Key columns

- Column `n_fnas` — verification category `derived` registry seed
- Column `n_bethesda_calculated` — verification category `derived` registry seed
- Column `n_nondiagnostic` — verification category `derived` registry seed
- Column `first_fna_date` — verification category `adjudicated` registry seed
- Column `last_fna_date` — verification category `adjudicated` registry seed
- Column `worst_bethesda_num` — verification category `derived` registry seed
- Column `best_bethesda_num` — verification category `adjudicated` registry seed
- Column `bethesda_final` — verification category `derived` registry seed
- Column `bethesda_final_name` — verification category `derived` registry seed
- Column `bethesda_index_nodule` — verification category `adjudicated` registry seed

## Known limitations

- *(No CF-* tokens found in registry notes — limitations may appear in prose only.)*


## Verification methods used

- `auto_identifier_skip`
- `auto_provenance_skip`
- `mechanical_derivation_compare against verified events table`

---
_Starter generated by `render_mig197_data_dictionary_readonly.py` (thyroid_canonical_publication_v1_0). Logan refines voice._
