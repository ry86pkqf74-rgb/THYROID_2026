  FATAL: --md requested but only MD_READ_SCALING_TOKEN (read-only) is set.
  Staging, promotion, and validators require MD_SA_TOKEN or MOTHERDUCK_TOKEN.

exit_code=1
(Subprocess: cwd=/tmp, RW env vars unset, MD_READ_SCALING_TOKEN set to a non-secret placeholder; no .streamlit/secrets.toml on path — simulates read-scaling-only credential surface. Correct refusal = PASS.)
