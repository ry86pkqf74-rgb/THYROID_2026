# MotherDuck live release audit (2026-04-10)

Tracked files here are **small** logs, SQL, scorecards, and validation excerpts from `scripts/124_md_live_release_gate.py` (tag `20260410`).

**Not committed** (see root `.gitignore`): `promotion_gate/llm_side_by_side.*`, promotion-gate and top-level `manual_review_queue.csv` — large; regenerate when needed.

**Entry point:** `audit_summary.md` and `validation_run/validation_report.md`.
