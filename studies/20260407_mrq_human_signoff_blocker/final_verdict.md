# Final verdict — real MRQ human signoff attempt

**Result:** **BLOCKED — Branch A** (incomplete / non-compliant adjudication package for manuscript claims).

## What was requested

Replace synthetic MRQ verification with **real human-reviewed signoff** when a compliant package exists; otherwise **stop** and emit a blocker (no “ready” overclaim). If complete: append decisions, run `126` **without** `--synthetic-fill-mrq-verification`, re-run `119 --release-mode`, refresh evidence.

## What we found

1. **Only plausible gate with full non-null MRQ + decisions CSV:** `studies/20260407_tier_policy_review_gate/`, generated by automation (`128`), not row-level human review for 5,620 fill rows.
2. **Policy gap:** `docs/domain_mapping_rules.md` requires **critical-tier** spot-check sampling and **documentation in `qa.promotion_review_decisions`**. The repo CSV has a **single** summary row — **does not satisfy** that requirement for **1,646** critical-tier rows.
3. **Live MotherDuck:** `qa.promotion_review_decisions` shows **append-only history** (small row count). `qa.manual_review_queue` has **no** synthetic status but **duplicate** 5,622-row cohorts under two `run_label`s — **not** a clean single snapshot.
4. **Branch B not executed** — no final release, no evidence pack refresh, **no** `--synthetic-fill-mrq-verification` in a publication path.

## Proof posture (explicit)

| Claim | Status |
|--------|--------|
| Synthetic MRQ gone in tier-policy **CSV** | Yes (0 `SYNTHETIC_*` in that gate) |
| Synthetic MRQ gone **live** | Yes (0 live in probe) |
| `pending_verification = 0` (`verification_status` NULL) live | Yes in probe |
| “Real human reviewer history” sufficient for **manuscript** sign-off | **No** — bulk `auto_accepted_*` + missing critical sample documentation |
| `release_<date>` exists | **Not evaluated** (final release not run) |
| Manuscript-signoff language “actually true” | **No** — stop here |

## Acceptance mapping

- **No overclaiming:** this folder stops the release narrative.
- **No synthetic-fill flag on final path:** N/A — final path not run.
- **Append-only reviewer history:** preserved on MotherDuck; we did not insert additional decision rows in this session.
