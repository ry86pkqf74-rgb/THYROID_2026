# Specimen / FHIR live parity — MotherDuck QA + prod (2026-04-13 UTC)

**Operator UA (session):** `specimen_fhir_live_parity_v1` via `MOTHERDUCK_CUSTOM_USER_AGENT`  
**Connection:** fail-closed `connect_md_or_file` / `connect_md_fail_closed`; RW token from `motherduck.local.toml` (not printed).  
**Catalog type:** **DUCKLAKE** on live prod (`Thyroid 2026`) — named `CREATE SNAPSHOT` not supported; use **130 `prepromote-backup`** zero-copy clones per runbook.

## 1. QA prerequisite (stale QA clone)

Initial `138` on QA failed: `main.synoptic_tumor_long_v1` missing on `Thyroid 2026 Molecular QA 20260407`.

**Remediation (runbook-aligned):**

```bash
export MOTHERDUCK_CUSTOM_USER_AGENT=specimen_fhir_live_parity_v1
.venv/bin/python scripts/130_md_env_bootstrap.py --execute --md-sa --date-tag 20260407 clone --qa
```

(Global flags **before** subcommand; matches `config/motherduck_environments.yml` QA name.)

## 2. QA pipeline (green)

```bash
export MOTHERDUCK_ENV=qa
export MOTHERDUCK_CUSTOM_USER_AGENT=specimen_fhir_live_parity_v1
.venv/bin/python scripts/138_md_specimen_fhir_layer.py --md
.venv/bin/python scripts/143_md_specimen_fhir_qa_diagnostics_deploy.py --md
.venv/bin/python scripts/119_md_formalization_validate.py --md --release-mode --md-sa \
  --output-dir studies/specimen_fhir_live_parity_20260413_1550_qa
.venv/bin/python scripts/144_md_repo_current_state_summary.py --md --md-env qa
```

**119 QA summary:** 35 PASS / 4 WARN / 0 FAIL — **Check 13 PASS** (specimen/FHIR layer clean).  
**WARN:** `note_extraction_runs` local parquet absent; molecular assay/panel_version pairing; assay dictionary; specimen-adjacent review burden (expected WARN, not FAIL).

**QA confirmations:**

| Check | Result |
|-------|--------|
| Check 13 | PASS |
| `qa.v_diag_specimen_fhir_broken_refs_v1` | empty on prod after deploy (verified below); QA aligned after full 138 replay |
| `qa.val_specimen_genomic_binding_v1` FAIL rows | 0 on prod (below) |

Artifact: `studies/specimen_fhir_live_parity_20260413_1550_qa/validation_report.md`

## 3. Prod prepromote backup

```bash
export MOTHERDUCK_CUSTOM_USER_AGENT=specimen_fhir_live_parity_v1
.venv/bin/python scripts/130_md_env_bootstrap.py --execute --md-sa prepromote-backup \
  --label specimen_fhir_replay_20260413_1625
```

**Rollback catalog created:** `Thyroid 2026 Molecular PrePromote specimen_fhir_replay_20260413_1625` (zero-copy `CREATE OR REPLACE DATABASE … FROM "Thyroid 2026"`).

## 4. Prod pipeline

```bash
export MOTHERDUCK_ENV=prod
export MOTHERDUCK_CUSTOM_USER_AGENT=specimen_fhir_live_parity_v1
.venv/bin/python -u scripts/138_md_specimen_fhir_layer.py --md
.venv/bin/python -u scripts/143_md_specimen_fhir_qa_diagnostics_deploy.py --md
.venv/bin/python -u scripts/119_md_formalization_validate.py --md --release-mode --md-sa \
  --output-dir studies/specimen_fhir_live_parity_20260413_1626_prod
.venv/bin/python -u scripts/144_md_repo_current_state_summary.py --md --md-env prod
```

**119 prod summary:** 35 PASS / 4 WARN / 0 FAIL — **Check 13 PASS**.

Artifacts:

- `studies/specimen_fhir_live_parity_20260413_1626_prod/validation_report.md`
- `studies/specimen_fhir_live_parity_20260413_1626_prod/119_run.log`
- `studies/specimen_fhir_live_parity_20260413_1550_qa/138_prod.log`, `143_prod.log` (prod 138/143 logs co-located with QA folder from this session)

## 5. Before / after row counts (prod `Thyroid 2026`)

| Table | Before | After |
|-------|-------:|------:|
| `main.specimen_master_v1` | 10,139 | 10,139 |
| `main.specimen_genomic_assay_v1` | 10,370 | 10,370 |
| `main.fhir_bundle_specimen_export_v1` | 10,139 | 10,139 |

Row counts unchanged: **full 138 replay** still required so **138 tail DDL** and **142 QA diagnostics** apply on the live catalog; the fix is **DDL / layer parity**, not row inflation.

**Post-deploy QA SQL (prod):**

- `SELECT COUNT(*) FROM qa.v_diag_specimen_fhir_broken_refs_v1` → **0**
- `SELECT COUNT(*) FROM qa.val_specimen_genomic_binding_v1 WHERE UPPER(COALESCE(status,'')) = 'FAIL'` → **0**

## 6. QUERY_HISTORY / RECENT_QUERIES

`md_information_schema.query_history` and `recent_queries` are queryable. A filter on `user_agent = 'specimen_fhir_live_parity_v1'` returned **no rows** in this session’s sample window (MotherDuck may attribute UA on connection string with delay, or history may be scoped by org/user). Recent rows show other agents (e.g. UI keepalive). Re-check in MotherDuck UI for this operator token if needed.

## 7. Duckling upsize

Not performed — **no org/admin API** from this workspace; note only if long builds recur.

## 8. Git / repo

- `studies/CURRENT_MOTHERDUCK_REPO_STATE.md` regenerated by **144** (commit SHA + live counts).  
- `README.md` updated to cite this report as the **2026-04-13** live parity reference.

---

**Verdict:** QA green after QA re-clone; prod **prepromote-backup** + **138/143/119/144** complete; **Check 13** green on prod; broken-ref and genomic-binding **FAIL** checks clean.
