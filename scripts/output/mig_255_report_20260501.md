# mig_255 recurrence flag vs timing

mig_255: NULL biochemical TTR on canonical_recurrence_v1 when recurrence_confirmed=FALSE (excl path_proven); resync PM TTR; flip any_recurrence_flag for path_proven gaps (~46). Pre mismatch PM=740; post mismatch PM=0. Closes CF-mig255-RECUR-FLAG-TIMING.

## Pre probes

- PM mismatch (flag FALSE, TTR non-null): **740**
- CR candidates for TTR NULL (not path_proven): **720**
- path_proven + any_recurrence_flag FALSE: **46**

## Lineage

- `time_to_recurrence_days` on PM: **canonical_recurrence_v1** (mig_139 resync).
- `any_recurrence_flag`: **mig_163b HYBRID** — `recurrence_confirmed` UNION `path_proven`.
- Table `canonical_recurrence_events_v1` **does not exist** on publication DB; use `canonical_recurrence_v1` + `canonical_recurrence_resolved_v1`.

## Post verify

- PM mismatch after fix: **0** (expect 0)
- CR residual FALSE+TTR: **20** (expected ~path_proven biochemical)
