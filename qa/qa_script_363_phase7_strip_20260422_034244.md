# Script 363 Phase 7 Cascade Strip — Audit Report

**BUILD_TS:** `20260422_034244`
**Run command:** `python scripts/363_invasion_canonical.py --commit --phase 7`
**Tip of `main` at strip time:** `396acb0` (CPM repoint commit)

## Pre-Strip Safety Gates (all PASS)

| Gate | Status | Detail |
|---|---|---|
| 7.1 Pre-strip archive snapshot | ✓ | `archive_pub_v1_0.canonical_operative_events_v1_pre363strip_20260422_034244` (11,773 rows) |
| 7.2.1 invasion_events_v1 nonzero | ✓ | 51,773 rows |
| 7.2.2 invasion_patient_rollup_v1 nonzero | ✓ | 10,871 rows |
| 7.2.3 preservation gates passed | ✓ | per `qa/qa_script_363_invasion.json` (16/16 from build) |
| 7.2.4 CPM feeder repoint marker | ✓ | `.invasion_cpm_repoint_applied` present (commit `396acb0`) |
| 7.2.5 forbidden cols not in STRIP list | ✓ | `strap_muscle_involvement_flag` correctly excluded |
| 7.2.6 dependent view scan | ✓ | 0 views reference dropped cols (or auto-refreshed) |

## Columns DROPPED from `main.canonical_operative_events_v1`

```
ALTER TABLE main.canonical_operative_events_v1 DROP COLUMN gross_ete_flag
ALTER TABLE main.canonical_operative_events_v1 DROP COLUMN tracheal_involvement_flag
ALTER TABLE main.canonical_operative_events_v1 DROP COLUMN esophageal_involvement_flag
ALTER TABLE main.canonical_operative_events_v1 DROP COLUMN local_invasion_flag
```

## Views refreshed (Pattern: ALTER doesn't cascade into view bodies)

```
CREATE OR REPLACE VIEW views_readable.invasion_events_VIEW_v1 AS SELECT * FROM main.canonical_invasion_events_v1
CREATE OR REPLACE VIEW views_readable.invasion_patient_rollup_VIEW_v1 AS SELECT * FROM main.canonical_invasion_patient_rollup_v1
CREATE OR REPLACE VIEW views_readable.Surgery_Episode_Detail AS SELECT * FROM main.canonical_operative_events_v1
```

## Post-Strip Verification — ALL GREEN ✓

### 1. Dropped cols absence check

| column | status |
|---|---|
| `gross_ete_flag` | DROPPED ✓ |
| `tracheal_involvement_flag` | DROPPED ✓ |
| `esophageal_involvement_flag` | DROPPED ✓ |
| `local_invasion_flag` | DROPPED ✓ |

### 2. Row-count parity

| table | row count | expected | status |
|---|---:|---:|---|
| `main.canonical_operative_events_v1` | 11,773 | 11,773 | ✓ unchanged |
| `main.canonical_invasion_events_v1` | 51,773 | 51,773 | ✓ unchanged |
| `main.canonical_invasion_patient_rollup_v1` | 10,871 | 10,871 | ✓ unchanged |

### 3. Q3 forbidden-from-drop guard

`strap_muscle_involvement_flag` still present on `canonical_operative_events_v1` (415 TRUE) — preserved per Q3 (intra-op surgeon-only finding; doesn't fit the cross-modal canonical pattern).

### 4. Remaining BOOL flags on `canonical_operative_events_v1` (zero-drift)

| BOOL column | n_TRUE |
|---|---:|
| `berry_ligament_flag` | 514 |
| `central_neck_dissection_flag` | 2,495 |
| `drain_flag` | 393 |
| `episode_has_cpm_anchor` | 11,649 |
| `frozen_section_any_deferred_flag` | 453 |
| `frozen_section_any_malignant_flag` | 773 |
| `frozen_section_any_suspected_malignant_flag` | 195 |
| `frozen_section_flag` | 4,226 |
| `lateral_neck_dissection_flag` | 241 |
| `parathyroid_autograft_flag` | 139 |
| `parathyroid_resection_flag` | 0 |
| `reoperative_field_flag` | 138 |
| `rln_monitoring_flag` | 3,663 |
| `strap_muscle_involvement_flag` | 415 |

14 remaining BOOL flags; all values unchanged (the strip only ALTER DROPPED the 4 invasion flags; no other column was modified).

### 5. Zero-grep view DDL gate

`0` views in `views_readable` reference any of the 4 dropped column names. ✓

### 6. Dependent view resolution

| view | status |
|---|---|
| `views_readable.invasion_events_VIEW_v1` | ✓ resolves |
| `views_readable.invasion_patient_rollup_VIEW_v1` | ✓ resolves |
| `views_readable.Surgery_Episode_Detail` | ✓ resolves (refreshed post-strip) |

## Reversibility

The strip is reversible via the pre-strip archive snapshot. To restore the 4 dropped columns:

```sql
-- Inspect snapshot
SELECT * FROM "Thyroid 2026 UPdated".archive_pub_v1_0.canonical_operative_events_v1_pre363strip_20260422_034244 LIMIT 5;

-- Re-add columns (one at a time) and backfill from snapshot
ALTER TABLE main.canonical_operative_events_v1 ADD COLUMN gross_ete_flag BOOLEAN;
UPDATE main.canonical_operative_events_v1 cur
SET gross_ete_flag = arch.gross_ete_flag
FROM "Thyroid 2026 UPdated".archive_pub_v1_0.canonical_operative_events_v1_pre363strip_20260422_034244 arch
WHERE cur.surgery_episode_id = arch.surgery_episode_id;
-- (repeat for tracheal_involvement_flag, esophageal_involvement_flag, local_invasion_flag)
```

This restoration path is documented for emergency rollback only; the canonical going forward is `canonical_invasion_events_v1` / `canonical_invasion_patient_rollup_v1`.

## Downstream consumer impact

CPM feeders for the 4 dropped flags were repointed to
`canonical_invasion_patient_rollup_v1.any_<type>_anywhere` columns in
the prior commit (`396acb0`). 8 BOOL CPM cols repointed; 7 new BOOL
CPM cols added for v3 invasion_types not previously surfaced.

Build scripts in `scripts/` that previously referenced the dropped
flags (60+ files for `gross_ete_flag` per `git grep`) will fail on next
run if invoked without modification. These are mostly historical /
deprecated build scripts (e.g., `22_canonical_episodes_v2.py`,
`104_operative_truth_state_hardening.py`, `327_operative_episode_detail_v2_rebuild.py`).
The active build pipeline (`scripts/362_operative_consolidation.py`,
`scripts/363_invasion_canonical.py`) does NOT reference the dropped
flags after this strip.

If a historical script needs to be re-run, restore the columns from
the pre-strip snapshot above first.
